spring:
  datasource:
    username: postgres
    url: jdbc:postgresql://localhost:5432/product_db
    password: '12345'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      show-sql: 'true'
      ddl-auto: update
